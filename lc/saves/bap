sub bang {
    my $self = shift;

    # Type something interesting for me to do here..............
    if (rand() < 0.5) {
      $self->trigger("/slub/samples/arp/arp2.wav");
    }
    else {
      $self->trigger("/slub/samples/arp/arp2.wav");
    }
    $self->modified;
}

